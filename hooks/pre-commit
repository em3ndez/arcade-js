#!/usr/bin/env bash
# Review gate (top-level rule: ALL commits reviewed). Blocks any commit whose staged
# changes lack a PASS review token bound to their exact content. See tools/review_gate.py.
# --no-verify bypasses this and defeats the rule; do not.
exec python3 "$(git rev-parse --show-toplevel)/tools/review_gate.py" check
