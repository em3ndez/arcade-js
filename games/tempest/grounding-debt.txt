# Grounding debt: honestly-irreducible ungrounded items (one "0xADDR  reason" per line).
# done_gate.check_grounding SUBTRACTS these from the ungrounded count. Each is independently
# reviewed (reviewer-rules R39) as un-groundable on a good ROM in any reachable state.
0xd8ca  power-on RAM-march self-test error-tone entry (playPowerOnTone): reached ONLY from the zero-page/stack RAM marching-pattern test's mismatch branch (a RAM cell reading back wrong = faulty RAM); never taken on a good RAM/emulation image -- a dead error arm.
0xd8cd  shared POKEY tone-burst driver for the three RAM-fault error-tone paths (d8ca/d92f/d931); has no non-fault caller, so reached only on a RAM readback mismatch -- dead error arm on a good image.
0xd92f  RAM-march pattern-verify fault tone (foldToneTableByte): reached only at the self-test pattern-verify mismatch branch (faulty RAM); never taken on a good RAM/emulation image.
0xd931  RAM-march fault tone-seed (seedToneBurstCount): reached only on a self-test RAM readback mismatch; never taken on a good RAM/emulation image -- dead error arm.
0x9ef1  Dead alternate enemy-mover variant (advanceEnemyPursuit): has no direct caller; dispatched ONLY via the object-motion-script mover-index table a0f7[y]=0x1e through the loc_9b98 rts-dispatcher (index 0x1e -> table word 0x9ba2+0x1e=0x9bc0 -> 0x9ef1). 0x1e appears in a0f7 only at y=0x61, but the script-index cell 0x010b never exceeds 0x5f (the script length): an 80k-frame value histogram over deep combat shows 0x010b takes every value 0x00-0x5f contiguously plus the 0xff terminator, never 0x60+. So a0f7[0x61] is past the live range and 9ef1 is never selected. Never executes on a good ROM (0 own-body writes across 120k+ frames, 5 levels, multiple bot strategies). The live flipper-movers are loc_9d82/loc_9cb6.
0x9f5f  Dead alternate per-slot fire-gate (maybeFireEnemyStep): its ONLY caller is the dead loc_9ef1 (grep confirms no other m.call/dispatch); reached only when the unselected pursuit mover runs, so dead on a good ROM.
0x9f8a  Dead alternate lane-flip step (flipEnemyLaneTowardTarget, random-seed entry): its ONLY callers are the dead loc_9ef1 and loc_9f5f (both dead); the LIVE flip is loc_9f81 via loc_9d82. Dead on a good ROM.
