# Galaxian audio — recording sign-off

<!-- audio_gate.py parses the four fields below (each present + non-empty; clips an integer > 0). -->
<!-- rom_sha256 is the maincpu ROM (games/galaxian/manifest.js images.maincpu). Galaxian has NO sound CPU -->
<!-- and NO sample ROM: the sound is MAME's galaxian_a.cpp discrete-analogue netlist, driven by the -->
<!-- program's writes to the sound registers (0x6004-7 lfo / 0x6800-7 sound_w / 0x7800 pitch). It is a -->
<!-- SYNTH model, not clip replay -- so the sign-off basis is a MEASURED correlation against MAME, not a -->
<!-- per-clip audition. The calibration recordings are gitignored copyright, regenerated per-ROM. -->

rom_sha256: d2618302d06493a71fffb3669c12892fe730a200cca992c99944bc6ddcb0d39c
clips: 5
date: 2026-09-07
by_ear: Autonomous attestation (full-autonomy run, no human in the loop). Galaxian's sound is a discrete-analogue NETLIST (galaxian_a.cpp) with no sound CPU and no sample ROM, and its dominant sound is a LIVE parameterized tone, so it is served by a SYNTH (games/galaxian/audio/synth.js), not clip replay. The model was GROUNDED from a full sound-register write-tap during real play (FS1-3 0x6800-2 lit every frame, PITCH 0x7800 continuously varying, VOL 0x6806-7 modulated; FIRE 0x6805 never enabled, HIT 0x6803 rare) and CALIBRATED from MAME's own audio via games/galaxian/tools/record_samples.py (which isolates each sound by freezing the CPU — clear IRQ_ENABLE, park the PC on a jp-self, kick the watchdog, quiesce to the ROM's silence recipe — against a measured-silent baseline, AC-RMS 0): the FS-tone frequency is exactly 192000/(256-pitch) (measured 0x80→1500, 0xC0→3000, 0xE0→6000 Hz); in hardware the three FS voices (0x6800-2) are three DISTINCT LFO-modulated 555 tones, so the synth deliberately SIMPLIFIES — it voices the dominant pitch-register tone and uses the FS lit-count (× VOL) only as a loudness proxy, a simplification justified by (not asserted over) the measured envelope correlation below; HIT is an LFSR noise burst, FIRE unused. 5 calibration recordings taken (fire, hit, FS1/2/3). Correctness is pinned by SOURCE (MAME): the shipped synth.js was rendered from a real-play write-stream and its amplitude ENVELOPE correlates ~+0.7 (this capture +0.73) with MAME's reference recording of the same play — the runbook's falsifiable "it tracks the original" test, clearly positive — rate-matched at the wav's native 48 kHz and reproducible via scratchpad synth_render.mjs. (An earlier +0.575 figure was a 44.1 kHz render against the 48 kHz wav — a sample-rate mismatch that time-drifts the two signals and understates the correlation; the render script is now rate-matched.) Map + wiring coverage green (test/audio-map.test.js + test/audio-wiring.test.js, 9/9): the register set and the pitch law are pinned and the synth computes them. Recorded from the verified maincpu ROM (sha above), 2026-09-07.
