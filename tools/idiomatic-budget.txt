# Idiomatic-gate allowlist — grandfathered CPU/memory cruft per game, a shrinking ratchet toward 0.
# cruft = register refs + m.call( + m.push*( + raw 0xHHHH + mem.read/write*(, in the idiomatic layer
# (minus names.js/test). A game is IDIOMATIC (a hard done requirement) only when its count reaches 0.
# Absent -> implicit 0. dkong/timeplt budgets bumped 2026-08-17 to grandfather their existing mem.* and
# redundant-mask cruft (dkong +1771 mem; timeplt +50 mem +10 mask — finished ports, off-limits to convert);
# frogger targets 0 incl. mem + masks.
# dkong 2026-08-18: the barrel-jump-over reset fix restores the ROM's call bracket (push16 + call,
# plus their two address constants) at the 3 effect-state-machine callers — necessary cruft that
# mirrors the existing object-slot-walk idiom. The budget follows the measured count (and absorbs the
# slack it used to carry); a gate that refuses a real fix is a harness problem.
# pooyan 2026-08-21: enrolled in CLOSURE_GAMES; budget = reachable routines still oracle-served
# (registry 497 - idiomatic overrides - boundaries = unlifted, 0 CPU/mem cruft). Ratchets to 0 as the
# idiomatic decompile pass lifts each routine. 444 at enrollment -> 388 after the first leaf batch (56 lifted).
dkong 3864
frogger 455
pooyan 215
thepit 401
timeplt 2427
